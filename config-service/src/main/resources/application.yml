# -------------------------------------------------------
# config 설정
# -------------------------------------------------------
spring:
  profiles:
    active: native
  application:
    name: config-service
  cloud:
      config:
        server:
          native:
            searchLocations: classpath:/configs

server:
  port: 8888