server:
  port: ${PORT:8080}

eureka:
  instance:
    prefer-ip-address: true
  server:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: https://eureka-service-dot-test-spring-cloud.appspot.com/eureka/
  datacenter: mycenter
  environment: test




welcome:
  message: I am eureka service