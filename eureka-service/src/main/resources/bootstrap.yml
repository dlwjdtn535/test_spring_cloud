spring:
  application:
    name: eureka-service
  cloud:
    config:
      uri: http://localhost:18888
      fail-fast: true

---
spring:
  profiles: gae
  cloud:
    config:
     uri: https://config-service-dot-nklee-project.appspot.com
     failFast: true